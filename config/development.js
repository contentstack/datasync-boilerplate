export const config = {
  // By default, we'd be using contentstack-content-store-filesystem, the following keys can be configured for it. To use, kindly uncomment it
  // contentStore: {
  //     baseDir: './_contents'
  // }
}
